/**
 * 根目录常量 re-export
 * 
 * [M-004] AGENTS 数据已移入 config/constants.ts 集中管理
 * 此文件保留为向后兼容的 re-export
 * 
 * @deprecated 请直接从 './config/constants' 导入
 */
export { DEFAULT_AGENTS as AGENTS } from './config/constants';
